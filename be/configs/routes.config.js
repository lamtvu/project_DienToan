const auth = '/auth';
const users = '/users';
const tables = '/tables';
const data = '/data';
const api = '/api'
const key = '/key'

module.exports = {
    auth,
    users,
    tables,
    data,
    api,
    key
}